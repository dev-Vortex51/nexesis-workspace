# 3.3: Document Viewer Backend

## Type

BACKEND ONLY — no frontend code

## Task

Implement document retrieval endpoints. Return document metadata + download URL. Version-specific retrieval. Add tests.

## Specs to Read

| File | Sections |
|------|----------|
| `03-workflow-spec.md` | N/A |
| `04-data-model.md` | Document, DocumentVersion |
| `05-api-spec.md` | GET /documents/:id, GET /documents/:id/versions/:versionNumber |

## Files to Create/Modify

- server/routes/documents.ts (update)

## Constraints

- Do not invent requirements not in the specs.
- Do not modify `components/ui/*`.
- Run `npm run build` before finishing.
- Update `progress-tracker.md` when done.
