# 1.3: Academic Session CRUD

## Type

BACKEND ONLY — no frontend code

## Task

Implement academic session CRUD. Activate/close logic. Deadline settings in JSONB. Coordinator/admin access. Add tests.

## Specs to Read

| File | Sections |
|------|----------|
| `03-workflow-spec.md` | N/A |
| `04-data-model.md` | AcademicSession entity |
| `05-api-spec.md` | GET /sessions, POST /sessions, GET /sessions/:id, PATCH /sessions/:id, POST /sessions/:id/activate, POST /sessions/:id/close |

## Files to Create/Modify

- server/routes/sessions.ts
- server/services/session.service.ts

## Constraints

- Do not invent requirements not in the specs.
- Do not modify `components/ui/*`.
- Run `npm run build` before finishing.
- Update `progress-tracker.md` when done.
